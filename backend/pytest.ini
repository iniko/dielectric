[pytest]
# The library raises UserWarning on suspect data (sign convention, band limits); the backend
# suppresses/surfaces these itself, so don't promote them to errors in API tests.
filterwarnings = default
addopts = -q
